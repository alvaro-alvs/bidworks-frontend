---
import Layout from "@/layouts/Layout.astro";
import getUserCookie from "@/services/getUserCookies.service";

const cookies = Astro.cookies

const usuario: any = await getUserCookie({cookies: cookies})

---

<Layout title={`Perfil ${usuario?.first_name + " " + usuario?.last_name} - bidWorks`}>
    <section class="flex flex-col justify-center items-center py-24 w-full">
        
        <div class="flex flex-col gap-10 w-full h-full sm:p-10 p-5">
            
            <h1 class="text-3xl">Olá {usuario?.first_name + " " + usuario?.last_name}!</h1>
            
        </div>
    </section>
</Layout>